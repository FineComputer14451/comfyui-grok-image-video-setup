# input/

Drop **Grok Imagine** stills here (PNG/JPG).

They appear in ComfyUI **Load Image** nodes. Prefer clear names:

```
input/hero_01.png
input/hero_02_face.png
```

This folder is bind-mounted into the container (`./input` → `/app/input`).
